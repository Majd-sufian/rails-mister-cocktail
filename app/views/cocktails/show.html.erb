
<nav class="navbar navbar-expand-lg navbar-light bg-light  sticky-top ">
<%= link_to "Delicious cocktails", cocktails_path , style: "color:#66DE89"%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <li class="nav-item active">
        <%= link_to "Home", cocktails_path , class:"nav-link"%>
      </li>
      <li class="nav-item">
        <%= link_to 'top 10' , top_cocktails_path ,class:"nav-link" %>
      </li>
        <p>
        <span id="banner-typed-text"></span>
        </p>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form>
  </div>
</nav>

<br>


<h1 class="text-center"><%= @cocktail.name %> <span class="badge badge-secondary text-underline">New</span></h1>

<br>
  <% if @cocktail.picture? %>
    <%= image_tag @cocktail.picture, class: "rounded mx-auto d-block" , height:350  %>
  <% end %>

  <br>

  <div class="pad10"> <% if @cocktail.doses.empty? %> <p class="text-gray text-center"> Rub the rim of the glass with the lime slice to make the salt stick to it.
  <br> Take care to moisten only the outer rim and sprinkle the salt on it. <br> The salt should present to the lips of the imbiber and never mix into the cocktail.
  <br> Shake the other ingredients with ice, then carefully pour into the glass. </p>
  <% else %>
   <h3 class="text-gray text-center"> Ingredients: </h3> <% end %>
  </div>

<br>
  <ul class="text-gray text-center">
    <% @cocktail.doses.each do |d| %>
    <lo>
      <i class="fa fa-glass"></i> <strong> <%= d.ingredient.name %> </strong>
    </lo>
    <p class="pad10"><strong> <%= d.description %></p> </strong>

    <%= link_to "", dose_path(d), class: "fas fa-trash", method: :delete ,
      data: { confirm: "Are you sure?" }, style: "color:#66DE89" %>
    <% end %>
    </div>
  </ul>

  <div class="container">
    <div class="row">
    <%= link_to "Add ingredient", new_cocktail_dose_path(@cocktail), class: "btn btn-secondary col ml-1", id: "ingredient-add-button"%>
    </div>
  </div>
  </div>

<br>

